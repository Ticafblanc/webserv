name: CI

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macOS-latest

    steps:
      - name: clean project
        run: |
          make finsih
          
      - name: compile project
        run: |
          make
          
      - name: recompile project
        run: |
          make re

      - name: Run webserv
        run: |
          make run
